const options = {
  xAxis: {
    splitLine: {
      lineStyle: {
        type: 'dashed'
      }
    }
  },
  yAxis: {
    splitLine: {
      lineStyle: {
        type: 'dashed'
      }
    },
    scale: true
  },
  grid: {
    top: 50,
    right: 20,
    bottom: 20,
    left: 20,
    containLabel: true,
  },
};

export default options;