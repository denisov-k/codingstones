const options = {
    tooltip: {
        formatter: "{b}: {c}",
        extraCssText: "white-space:pre-wrap; max-width:400px;",
        confine: true,
    },
    series: [],
};

export default options;