const options = {
  grid: {
    top: 50,
    right: 20,
    bottom: 20,
    left: 20,
    containLabel: true,
  },
  tooltip: {
    trigger: 'item',
    triggerOn: 'mousemove'
  },
  series: [
  ]
};

export default options;